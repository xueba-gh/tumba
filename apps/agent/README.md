# Render agent (apps/agent)

Optional FastAPI service for 4K/batch rendering and `faster-whisper`
alignment, built in Phase 7 from the proven pipeline in
`docs/build-package/reference_code/`. Empty scaffold for now — see
`docs/build-package/03_BUILD_PROMPTS.md` Phase 7 for the full spec
(`/health`, `/align`, `/render`, `/job/{id}`, `/download/{id}`, bearer
auth, Dockerfile, run.bat/run.sh).
