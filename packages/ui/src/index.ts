// Shared shadcn/ui components + theme land here in a later phase, once the
// design system is settled. Deliberately empty for Phase 1 — apps/web uses
// plain Tailwind classes until then. See docs/DECISIONS.md.
export {};
